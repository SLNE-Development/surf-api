package dev.slne.surf.api.paper.display.behavior

/**
 * Attaches a tooltip to an element that shows when hovered.
 */
class TooltipBehavior(
    /** Tooltip text to display. */
    val text: String,
) : Behavior
